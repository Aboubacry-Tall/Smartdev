<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Génération des factures mensuelles le 1er de chaque mois -->
        <record id="ir_cron_generate_monthly_invoices" model="ir.cron">
            <field name="name">Contrats: générer les factures mensuelles</field>
            <field name="model_id" ref="model_wg_contract"/>
            <field name="state">code</field>
            <field name="code">model.cron_generate_monthly_invoices()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 02:00:00')"/>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
